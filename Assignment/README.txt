Use 'make' to compile, link, and produce the GUI

Selecting the ASSIGNMENT window with the topology, press the SPACEBAR to start

Use 'make clean' to clean *.o, *.cnet files that have been created as a result of making the file